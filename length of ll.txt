
int length(Node *head)
{
    //Write your code here
    int  count =0;
    if (head==NULL)
        return 0;
    Node *temp=head;
    while(temp->next!=NULL)
    {
        count ++;
        temp=temp->next;
    }	
    return count+1 ;
}
